base
# base-devel provides makepkg and the toolchain every AUR build needs.
base-devel
# Needed to clone the paru PKGBUILD. Without it afi-aur-setup cannot bootstrap
# an AUR helper at all.
git
btrfs-progs
cryptsetup
dosfstools
# chattr and lsattr, used to keep copy-on-write off the swapfile. Needed in the
# installed system too, or the attribute cannot be checked after a reboot.
e2fsprogs
gptfdisk
jq
linux
linux-firmware
linux-headers
linux-lts
linux-lts-headers
limine
mkinitcpio
nano
networkmanager
sudo
zram-generator
